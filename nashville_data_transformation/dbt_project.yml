name: nashville_data_transformation
version: 1.0.0
config-version: 2

profile: nashville_data_transformation

model-paths:
  - models

analysis-paths:
  - analyses

test-paths:
  - tests

seed-paths:
  - seeds

macro-paths:
  - macros

snapshot-paths:
  - snapshots

target-path: target

clean-targets:
  - target
  - dbt_packages

models:
  nashville_data_transformation:
    silver:
      +materialized: table

    gold:
      +materialized: table